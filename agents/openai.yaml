interface:
  display_name: "MinerU PDF 转 Markdown"
  short_description: "调用 MinerU 云 API 将复杂 PDF 转为结构化 Markdown"
  default_prompt: "Use $mineru-pdf-to-md to convert this PDF into verified Markdown."
policy:
  allow_implicit_invocation: true
